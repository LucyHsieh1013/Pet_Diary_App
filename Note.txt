後端執行位置:
    在backend/.env設定，如果.env未宣告在server.js內預設為http://localhost:3000
    如果要用區域網方式連線，在backend/.env設定，用Ipconfig看IPv4，在HOST位址填入192.168.X.X
前端連線到後端:
    如果要用區域網方式連線，在.env.client設定，與 backend/.env 內的HOST相同
    base_url.dart內會判斷裝置，預設一般為http://localhost:3000，而android模擬器預設為http://10.0.2.2:3000
    如果要連線到後端要使用import 'package:test_app/services/base_url.dart';
    Uri.parse('$BASE_URL/xxx/xxx')
登入:
    前端驗證:
        格式錯誤的即時顯示
        格式錯誤即不得送出表單(因測試用的帳號不符格式，因此帳號的表單送出驗證暫時註解，但即時驗證仍存在，即時驗證仍存在，但不影響登入)
    後端驗證:
        抓出資料庫整筆用戶資料，先檢查帳號是否存在，再比對密碼是否正確
    token:
        jwt的加密為'secretKey'並非亂碼加密(與未登入修改密碼不同)
        login.js配發登入token
        LoginServer.dart接收token並呼叫token.dart將token儲存至本地端(使用shared_preferences)
登出:
    token:
        登出按鈕在SettingPage.dart，登出時刪除token
        
未登入修改密碼:
    jwt的加密為'80a1c66e79443f97989d40933dab1721ac66b3838c88383f23f3525b71d2ca5a3058c7addcdf'亂碼加密
    必須在學校的局域網，如果在校外必須使用VPN
    送出email後會配發一組驗證碼，驗證碼送出後會配發token用作密碼修改

Provider: 用於讓同個物件不會因為切換畫面而一直重複加載
    登出時必須重置provider(provider.dart)，否則下次登入仍是上一位使用者的資料
    有重置provider，再次登入時才會再次呼叫provider(provider.dart)
    問題:
        使用者修改使用者介面資料時該如何應對?單獨重置某部分的provider?

未來待改善之問題:
    驗證畫面的驗證按鈕尚未使用標準化按鈕(因為格式需要修改所以有點懶改天再弄)
    登入及註冊在格式錯誤的情況下若嘗試送出表單，直到下一次送出表單，及時驗證都處於失效狀態

登入帳密(email非實際存在，無法用於接收驗證碼):
test 123456
Hsieh@gmail.com 123456

專案大架構
    Pet_Diary_App/
    ├── backend/                   <-- 後端專案資料夾 (Node.js)
    │   ├── router/                   <-- 路由處理資料夾 (如 auth.js, user.js)
    │   ├── server.js                 <-- 伺服器設定
    │   ├── db.js                     <-- 資料庫處理函式
    │
    ├── database/                  <-- 資料庫資料夾
    │   ├── database.db               <-- 資料庫檔案
    │
    ├── lib/                       <-- 前端 Flutter 資料夾
    │   ├── component/                <-- 公用元件資料夾
    │   ├── provider/                 <-- Flutter 的provider資料夾
    │   ├── screens/                  <-- Flutter 的畫面資料夾
    │   ├── services/                 <-- 用來儲存 Token 或執行 API 請求等邏輯
    │   ├── main.dart                 <-- 應用程式的入口點


CalenandarPage的事件資料目前存在本地端，尚未連接資料庫